<Window x:Class="KetabaPOS.Desktop.Presentation.Views.CategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Categories" Height="450" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Categories" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="CategoryNameBox" Width="240" Height="32" FontSize="14" />
            <Button Content="Add" Click="AddCategory_Click" Width="80" Height="32" Margin="12,0,0,0"
                    Background="#2563EB" Foreground="White" BorderThickness="0" FontWeight="Medium"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding}" x:Name="CategoryList">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,2" Padding="8,6" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" FontSize="14" VerticalAlignment="Center" />
                            <Button Grid.Column="1" Content="Delete" Click="DeleteCategory_Click"
                                    Background="#EF4444" Foreground="White" BorderThickness="0"
                                    FontSize="11" Height="26" Padding="12,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
