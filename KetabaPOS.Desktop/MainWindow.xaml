<Window x:Class="KetabaPOS.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:KetabaPOS.Desktop.Presentation.ViewModels"
        xmlns:converters="clr-namespace:KetabaPOS.Desktop.Presentation.Converters"
        mc:Ignorable="d"
        Title="Ketaba POS - Point of Sale System" Height="800" Width="1200"
        WindowState="Maximized"
        FontSize="13">
    <Window.Resources>
        <converters:ImagePathConverter x:Key="ImagePathConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:EnumToBoolConverter x:Key="EnumToBoolConverter" />

        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Dashboard" Style="{StaticResource PageHeaderStyle}" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" Margin="0,-16,0,16" />
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <UniformGrid Columns="4" Margin="0,0,0,24">
                            <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                                <StackPanel>
                                    <TextBlock Text="Today Sales" Style="{StaticResource KpiLabelStyle}" />
                                    <TextBlock Text="{Binding TodaySales, StringFormat={}{0:N2}}" Style="{StaticResource KpiValueStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                                <StackPanel>
                                    <TextBlock Text="Transactions" Style="{StaticResource KpiLabelStyle}" />
                                    <TextBlock Text="{Binding TodayTransactions}" Style="{StaticResource KpiValueStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                                <StackPanel>
                                    <TextBlock Text="Low Stock Items" Style="{StaticResource KpiLabelStyle}" />
                                    <TextBlock Text="{Binding LowStockProducts}" Style="{StaticResource KpiValueStyle}" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                                <StackPanel>
                                    <TextBlock Text="Active Loans" Style="{StaticResource KpiLabelStyle}" />
                                    <TextBlock Text="{Binding ActiveLoans}" Style="{StaticResource KpiValueStyle}" />
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                        <Border Style="{StaticResource CardStyle}" Margin="8">
                            <StackPanel>
                                <TextBlock Text="Recent Activity" Style="{StaticResource SectionHeaderStyle}" />
                                <ItemsControl ItemsSource="{Binding RecentActivities}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description}" Margin="0,4" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PosViewModel}">
            <Grid Margin="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="400"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Point of Sale" Style="{StaticResource PageHeaderStyle}" />

                    <!-- Barcode Scanner Bar -->
                    <Border Style="{StaticResource CardStyle}" Padding="12,8" Margin="0,0,0,12" Background="#F8FAFC">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Left" Text="Barcode:" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding ScanStatus}" VerticalAlignment="Center" FontSize="12" Foreground="#10B981" Margin="12,0,0,0"/>
                            <TextBox Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Height="34" Padding="4,0">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}" />
                                </TextBox.InputBindings>
                            </TextBox>
                        </DockPanel>
                    </Border>

                    <DockPanel Margin="0,0,0,16">
                        <Button DockPanel.Dock="Right" Content="Search" Command="{Binding SearchProductsCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0"/>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Products}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardStyle}" Margin="6" Width="170">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="80"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Border Grid.Row="0" Background="#F1F5F9" CornerRadius="8" Width="80" Height="80" HorizontalAlignment="Center">
                                                <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}" Width="72" Height="72" Stretch="Uniform" />
                                            </Border>
                                            <TextBlock Grid.Row="1" Text="{Binding Name}" FontWeight="Medium" FontSize="12" Margin="4,4,4,0" TextWrapping="Wrap" MaxHeight="32" />
                                            <TextBlock Grid.Row="2" Text="{Binding RetailPrice, StringFormat={}{0:N2}}" Foreground="#2563EB" FontSize="16" FontWeight="Bold" Margin="4,2,4,0" />
                                            <Button Grid.Row="3" Content="Add" Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButtonStyle}" Height="28" Margin="4,6,4,6" FontSize="12"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="16,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Cart" Style="{StaticResource SectionHeaderStyle}" />
                        <ItemsControl ItemsSource="{Binding CartItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,4" Padding="8" BorderThickness="0,0,0,1" BorderBrush="#E2E8F0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding ProductName}" FontWeight="Medium" />
                                                <TextBlock Text="{Binding Total, StringFormat={}{0:N2}}" Foreground="#2563EB" />
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="X" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Foreground="#EF4444" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Separator Margin="0,8" />
                        <TextBlock Text="{Binding Subtotal, StringFormat=Subtotal: {0:N2}}" />
                        <TextBlock Text="{Binding TaxAmount, StringFormat=Tax: {0:N2}}" />
                        <TextBlock Text="{Binding DiscountAmount, StringFormat=Discount: {0:N2}}" />
                        <TextBlock Text="{Binding TotalAmount, StringFormat=Total: {0:N2}}" FontSize="24" FontWeight="Bold" Foreground="#2563EB" />
                        <TextBox Text="{Binding PaidAmount, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}" Margin="0,16,0,0" />
                        <TextBlock Text="{Binding ChangeAmount, StringFormat=Change: {0:N2}}" />
                        <Button Content="Complete Sale" Command="{Binding CompleteSaleCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,16,0,0" Height="48" FontSize="16"/>
                        <Button Content="Print Receipt" Command="{Binding PrintReceiptCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,8,0,0" Height="36" Background="#10B981"/>
                        <Button Content="Clear Cart" Command="{Binding ClearCartCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#475569" Margin="0,8,0,0" TextWrapping="Wrap" FontSize="11"/>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ProductsViewModel}">
            <Grid Margin="24">
                <!-- List View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Products" Style="{StaticResource PageHeaderStyle}" Grid.Row="0" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#475569" Margin="0,-20,0,4" Grid.Row="0" />

                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,16">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" />
                        <ComboBox ItemsSource="{Binding Categories}" SelectedValue="{Binding SelectedCategoryId}" DisplayMemberPath="Name" SelectedValuePath="Id" Width="180" Margin="12,0,0,0" />
                        <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" />
                        <Button Content="+ Add Product" Command="{Binding ShowAddFormCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" Background="#10B981"/>
                        <Button Content="Categories" Command="{Binding ManageCategoriesCommand}" Style="{StaticResource ActionButtonStyle}" Margin="12,0,0,0" Background="#8B5CF6" Foreground="White"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Products}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Image" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}" Width="40" Height="40" Stretch="Uniform" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="120" />
                            <DataGridTextColumn Header="Price" Binding="{Binding RetailPrice, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Cost" Binding="{Binding CostPrice, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="70" />
                            <DataGridTextColumn Header="Min" Binding="{Binding MinStockLevel}" Width="60" />
                            <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="60" />
                            <DataGridTemplateColumn Header="Actions" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.ShowEditFormCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#3B82F6" Foreground="White" FontSize="11" Height="26"/>
                                            <Button Content="Delete" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#EF4444" Foreground="White" FontSize="11" Height="26"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,12,0,0" HorizontalAlignment="Center">
                        <Button Content="Previous" Command="{Binding PreviousPageCommand}" Style="{StaticResource ActionButtonStyle}" Margin="4"/>
                        <TextBlock Text="{Binding CurrentPage, StringFormat=Page {0}}" VerticalAlignment="Center" Margin="12,0" />
                        <Button Content="Next" Command="{Binding NextPageCommand}" Style="{StaticResource ActionButtonStyle}" Margin="4"/>
                    </StackPanel>
                </Grid>

                <!-- Form View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="{Binding FormTitle}" Style="{StaticResource PageHeaderStyle}" />

                            <TextBlock Text="Name" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormName}" />

                            <TextBlock Text="Name (Arabic)" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormNameAr}" />

                            <TextBlock Text="Barcode" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormBarcode}" />

                            <TextBlock Text="SKU" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormSku}" />

                            <TextBlock Text="Category" Margin="0,8,0,2" Foreground="#475569"/>
                            <ComboBox ItemsSource="{Binding Categories}" SelectedValue="{Binding FormCategoryId}" DisplayMemberPath="Name" SelectedValuePath="Id" />

                            <TextBlock Text="Cost Price" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormCostPrice}" />

                            <TextBlock Text="Retail Price" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormRetailPrice}" />

                            <TextBlock Text="Wholesale Price" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormWholesalePrice}" />

                            <TextBlock Text="Stock Quantity" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormStockQuantity}" />

                            <TextBlock Text="Min Stock Level" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormMinStockLevel}" />

                            <TextBlock Text="Unit (e.g. pcs, kg, box)" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormUnit}" />

                            <TextBlock Text="Description" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormDescription}" AcceptsReturn="True" Height="80" TextWrapping="Wrap" />

                            <TextBlock Text="Product Image" Margin="0,12,0,2" Foreground="#475569"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Border Width="100" Height="100" Background="#F1F5F9" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" HorizontalAlignment="Left">
                                    <Image Source="{Binding FormImagePreviewPath, Converter={StaticResource ImagePathConverter}}" Width="90" Height="90" Stretch="Uniform" />
                                </Border>
                                <StackPanel VerticalAlignment="Center" Margin="16,0,0,0">
                                    <Button Content="Select Image" Command="{Binding SelectImageCommand}" Style="{StaticResource ActionButtonStyle}" Background="#3B82F6" Foreground="White"/>
                                    <TextBlock Text="{Binding FormImagePath}" Margin="0,4,0,0" Foreground="#64748B" TextWrapping="Wrap" MaxWidth="300" FontSize="11" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,24">
                                <Button Content="Save" Command="{Binding SaveProductCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120" Height="40"/>
                                <Button Content="Cancel" Command="{Binding CancelFormCommand}" Style="{StaticResource ActionButtonStyle}" Width="120" Height="40" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CustomersViewModel}">
            <Grid Margin="24">
                <!-- List View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Customers" Style="{StaticResource PageHeaderStyle}" />
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,4">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" />
                        <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" />
                        <Button Content="+ Add Customer" Command="{Binding ShowAddFormCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" Background="#10B981"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Customers}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Name (Ar)" Binding="{Binding NameAr}" Width="150" />
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="160" />
                            <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Credit Limit" Binding="{Binding CreditLimit, StringFormat={}{0:N2}}" Width="100" />
                            <DataGridTemplateColumn Header="Actions" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.ShowEditFormCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#3B82F6" Foreground="White" FontSize="11" Height="26"/>
                                            <Button Content="Delete" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#EF4444" Foreground="White" FontSize="11" Height="26"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- Form View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="{Binding FormTitle}" Style="{StaticResource PageHeaderStyle}" />
                            <TextBlock Text="Name" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormName}" />
                            <TextBlock Text="Name (Arabic)" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormNameAr}" />
                            <TextBlock Text="Phone" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormPhone}" />
                            <TextBlock Text="Email" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormEmail}" />
                            <TextBlock Text="Address" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormAddress}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" />
                            <TextBlock Text="Credit Limit" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormCreditLimit}" />
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,24">
                                <Button Content="Save" Command="{Binding SaveCustomerCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120" Height="40"/>
                                <Button Content="Cancel" Command="{Binding CancelFormCommand}" Style="{StaticResource ActionButtonStyle}" Width="120" Height="40" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SuppliersViewModel}">
            <Grid Margin="24">
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Suppliers" Style="{StaticResource PageHeaderStyle}" />
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,16">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" />
                        <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" />
                        <Button Content="+ Add Supplier" Command="{Binding ShowAddFormCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" Background="#10B981"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Suppliers}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Name (Ar)" Binding="{Binding NameAr}" Width="150" />
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="160" />
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200" />
                            <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTemplateColumn Header="Actions" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit" Command="{Binding DataContext.ShowEditFormCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#3B82F6" Foreground="White" FontSize="11" Height="26"/>
                                            <Button Content="Delete" Command="{Binding DataContext.DeleteSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#EF4444" Foreground="White" FontSize="11" Height="26"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="{Binding FormTitle}" Style="{StaticResource PageHeaderStyle}" />
                            <TextBlock Text="Name" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormName}" />
                            <TextBlock Text="Name (Arabic)" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormNameAr}" />
                            <TextBlock Text="Phone" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormPhone}" />
                            <TextBlock Text="Email" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormEmail}" />
                            <TextBlock Text="Address" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormAddress}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,24">
                                <Button Content="Save" Command="{Binding SaveSupplierCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120" Height="40"/>
                                <Button Content="Cancel" Command="{Binding CancelFormCommand}" Style="{StaticResource ActionButtonStyle}" Width="120" Height="40" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SalesViewModel}">
            <Grid Margin="24">
                <Grid Visibility="{Binding ShowDetail, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Sales History" Style="{StaticResource PageHeaderStyle}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#475569" Margin="0,0,0,8" Grid.Row="0" />
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,16">
                        <DatePicker SelectedDate="{Binding FromDate}" Width="140" />
                        <DatePicker SelectedDate="{Binding ToDate}" Width="140" Margin="12,0,0,0" />
                        <Button Content="Filter" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" />
                    </StackPanel>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Sales}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNumber}" Width="140" />
                            <DataGridTextColumn Header="Customer" Binding="{Binding Customer.Name}" Width="*" />
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Payment" Binding="{Binding PaymentMethod}" Width="90" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" />
                            <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat={}{0:g}}" Width="130" />
                            <DataGridTemplateColumn Header="Actions" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="View" Command="{Binding DataContext.ViewSaleDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#3B82F6" Foreground="White" FontSize="11" Height="26"/>
                                            <Button Content="Print" Command="{Binding DataContext.PrintReceiptCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#10B981" Foreground="White" FontSize="11" Height="26"/>
                                            <Button Content="Cancel" Command="{Binding DataContext.CancelSaleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#EF4444" Foreground="White" FontSize="11" Height="26"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,12,0,0" HorizontalAlignment="Center">
                        <Button Content="Previous" Command="{Binding PreviousPageCommand}" Style="{StaticResource ActionButtonStyle}" Margin="4"/>
                        <TextBlock Text="{Binding CurrentPage, StringFormat=Page {0}}" VerticalAlignment="Center" Margin="12,0" />
                        <Button Content="Next" Command="{Binding NextPageCommand}" Style="{StaticResource ActionButtonStyle}" Margin="4"/>
                    </StackPanel>
                </Grid>

                <!-- Sale Detail -->
                <Grid Visibility="{Binding ShowDetail, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sale Detail" Style="{StaticResource PageHeaderStyle}" />
                        <Button Content="Back" Command="{Binding CloseDetailCommand}" Style="{StaticResource ActionButtonStyle}" Margin="16,0,0,24"/>
                    </StackPanel>
                    <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock FontSize="16" FontWeight="Bold" Text="{Binding SelectedSale.InvoiceNumber}" />
                            <TextBlock Text="{Binding SelectedSale.CreatedAt, StringFormat=Date: {0:yyyy-MM-dd HH:mm}}" />
                            <TextBlock Text="{Binding SelectedSale.Customer.Name, StringFormat=Customer: {0}}" />
                            <TextBlock Text="{Binding SelectedSale.PaymentMethod, StringFormat=Payment: {0}}" />
                        </StackPanel>
                    </Border>
                    <TextBlock Grid.Row="2" Text="Items" Style="{StaticResource SectionHeaderStyle}" />
                    <DataGrid Grid.Row="3" ItemsSource="{Binding SaleItems}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*" />
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60" />
                            <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, StringFormat={}{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat={}{0:N2}}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="0,0,16,0">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="Total: {0:N2} | Paid: {1:N2} | Change: {2:N2}">
                                    <Binding Path="SelectedSale.TotalAmount" />
                                    <Binding Path="SelectedSale.PaidAmount" />
                                    <Binding Path="SelectedSale.ChangeAmount" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:LoansViewModel}">
            <Grid Margin="24">
                <!-- List View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Loans &amp; Debts" Style="{StaticResource PageHeaderStyle}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,16">
                        <RadioButton Content="Customer Loans" IsChecked="{Binding SelectedLoanType, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=CustomerLoan}" Margin="0,0,16,0" GroupName="LoanType"/>
                        <RadioButton Content="Supplier Loans" IsChecked="{Binding SelectedLoanType, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=SupplierLoan}" Margin="0,0,16,0" GroupName="LoanType"/>
                        <Button Content="+ Add Loan" Command="{Binding ShowAddFormCommand}" Style="{StaticResource PrimaryButtonStyle}" Background="#10B981"/>
                    </StackPanel>

                    <!-- Payments Panel -->
                    <Grid Grid.Row="2">
                        <Grid Visibility="{Binding ShowPayments, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                            <DataGrid ItemsSource="{Binding Loans}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="100" />
                                    <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat={}{0:N2}}" Width="90" />
                                    <DataGridTextColumn Header="Remaining" Binding="{Binding RemainingAmount, StringFormat={}{0:N2}}" Width="100" />
                                    <DataGridTextColumn Header="Interest" Binding="{Binding InterestRate, StringFormat={}{0:N2}%}" Width="70" />
                                    <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat={}{0:d}}" Width="90" />
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" />
                                    <DataGridTemplateColumn Header="Customer/Supplier" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="{Binding Customer.Name}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Customer}" Value="{x:Null}">
                                                                    <Setter Property="Text" Value="{Binding Supplier.Name}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="150" />
                                    <DataGridTemplateColumn Header="Actions" Width="200">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Payments" Command="{Binding DataContext.ShowLoanPaymentsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#3B82F6" Foreground="White" FontSize="11" Height="26"/>
                                                    <Button Content="Mark Paid" Command="{Binding DataContext.MarkAsPaidCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#10B981" Foreground="White" FontSize="11" Height="26"/>
                                                    <Button Content="Delete" Command="{Binding DataContext.DeleteLoanCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Margin="2" Background="#EF4444" Foreground="White" FontSize="11" Height="26"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <!-- Payments View -->
                        <Grid Visibility="{Binding ShowPayments, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Payments" Style="{StaticResource PageHeaderStyle}" />
                                <Button Content="Back" Command="{Binding ClosePaymentsCommand}" Style="{StaticResource ActionButtonStyle}" Margin="16,0,0,24"/>
                            </StackPanel>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding LoanPayments}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="120" />
                                    <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate, StringFormat={}{0:g}}" Width="*" />
                                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                                <TextBox Text="{Binding PaymentAmount}" Width="150" />
                                <Button Content="Add Payment" Command="{Binding AddPaymentCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>

                <!-- Form View -->
                <Grid Visibility="{Binding ShowForm, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="{Binding FormTitle}" Style="{StaticResource PageHeaderStyle}" />
                            <TextBlock Text="Amount" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormAmount}" />
                            <TextBlock Text="Paid Amount" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormPaidAmount}" />
                            <TextBlock Text="Interest Rate (%)" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormInterestRate}" />
                            <TextBlock Text="Due Date" Margin="0,8,0,2" Foreground="#475569"/>
                            <DatePicker SelectedDate="{Binding FormDueDate}" />
                            <TextBlock Text="Notes" Margin="0,8,0,2" Foreground="#475569"/>
                            <TextBox Text="{Binding FormNotes}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,24">
                                <Button Content="Save" Command="{Binding SaveLoanCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120" Height="40"/>
                                <Button Content="Cancel" Command="{Binding CancelFormCommand}" Style="{StaticResource ActionButtonStyle}" Width="120" Height="40" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <Grid Margin="24">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="600">
                        <TextBlock Text="Settings" Style="{StaticResource PageHeaderStyle}" />

                        <!-- Company Information -->
                        <TextBlock Text="Company Information" Style="{StaticResource SectionHeaderStyle}" />
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Company Name" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding CompanyName}" Margin="0,0,0,12" />
                                <TextBlock Text="Company Name (Arabic)" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding CompanyNameAr}" Margin="0,0,0,12" />
                                <TextBlock Text="Address" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding CompanyAddress}" Margin="0,0,0,12" />
                                <TextBlock Text="Phone" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding CompanyPhone}" Margin="0,0,0,12" />
                                <TextBlock Text="Currency" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding Currency}" />
                            </StackPanel>
                        </Border>

                        <!-- Tax Settings -->
                        <TextBlock Text="Tax Settings" Style="{StaticResource SectionHeaderStyle}" />
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Tax Rate (%)" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding TaxRate}" />
                            </StackPanel>
                        </Border>

                        <!-- Receipt Customization -->
                        <TextBlock Text="Receipt Customization" Style="{StaticResource SectionHeaderStyle}" />
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Receipt Header Text" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding ReceiptHeader}" Margin="0,0,0,12" />

                                <TextBlock Text="Receipt Footer Text" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding ReceiptFooter}" Margin="0,0,0,12" AcceptsReturn="True" Height="50" TextWrapping="Wrap" />

                                <TextBlock Text="Paper Format" Margin="0,0,0,2" Foreground="#475569"/>
                                <ComboBox ItemsSource="{Binding PaperFormats}" SelectedItem="{Binding PaperFormat}" Margin="0,0,0,12" />

                                <TextBlock Text="Text Color (Hex)" Margin="0,0,0,2" Foreground="#475569"/>
                                <TextBox Text="{Binding ReceiptTextColor}" Margin="0,0,0,12" />

                                <TextBlock Text="Company Logo" Margin="0,0,0,2" Foreground="#475569"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Border Width="80" Height="80" Background="#F1F5F9" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
                                        <Image Source="{Binding LogoPreviewPath, Converter={StaticResource ImagePathConverter}}" Width="72" Height="72" Stretch="Uniform" />
                                    </Border>
                                    <StackPanel VerticalAlignment="Center" Margin="12,0,0,0">
                                        <Button Content="Select Logo" Command="{Binding SelectLogoCommand}" Style="{StaticResource ActionButtonStyle}" Background="#3B82F6" Foreground="White"/>
                                        <TextBlock Text="{Binding LogoPath}" Margin="0,4,0,0" Foreground="#64748B" TextWrapping="Wrap" MaxWidth="250" FontSize="11" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Database -->
                        <TextBlock Text="Database" Style="{StaticResource SectionHeaderStyle}" />
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <Button Content="Backup Database" Command="{Binding BackupDatabaseCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,8" Background="#10B981"/>
                                <Button Content="Restore Database" Command="{Binding RestoreDatabaseCommand}" Style="{StaticResource ActionButtonStyle}" Margin="0,0,0,8" Background="#F59E0B" Foreground="White"/>
                            </StackPanel>
                        </Border>

                        <Button Content="Save All Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="44" FontSize="16"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" Margin="0,12,0,24" FontWeight="Medium" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#2563EB" Padding="16,12">
            <DockPanel>
                <TextBlock Text="Ketaba POS" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Foreground="White" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Dashboard" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" />
                    <Button Content="POS" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Pos" />
                    <Button Content="Products" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Products" />
                    <Button Content="Customers" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Customers" />
                    <Button Content="Suppliers" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Suppliers" />
                    <Button Content="Sales" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Sales" />
                    <Button Content="Loans" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Loans" />
                    <Button Content="Settings" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Settings" />
                    <Separator Margin="8,0" Background="White" Opacity="0.5" Width="1" Height="20" />
                    <Button Content="Logout" Margin="4,0" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding LogoutCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Background="#F1F5F9">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Grid>
    </DockPanel>
</Window>
